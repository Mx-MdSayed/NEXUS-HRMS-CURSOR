/** Compatibility shim — prefer organization orgDb / services. */
export {
  listActiveDepartmentOptions as mockDepartments,
  listActiveDesignationOptions as getMockDesignations,
} from '@/features/organization/data/orgDb'
